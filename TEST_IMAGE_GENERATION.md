# 🧪 图像生成测试指南

## ✅ 已优化的问题

### 1. 扩展了触发关键词
现在包含更多关键词：
- 基础词：图片、图像、照片、截图、画、图
- 动作词：生成、画个、拍张、来张、发张、看张
- 询问词：图呢、有图吗、来图、发图、上图、配图、插图
- 查看词：看看、瞧瞧、展示、给我看

### 2. 添加了详细日志
打开浏览器控制台（F12），可以看到：
- 媒体类型检测日志
- API 调用状态
- 错误信息

### 3. 改进了错误提示
- 图片生成失败时会显示具体错误原因
- 不再静默失败

---

## 📝 测试步骤

### 方法 1：使用关键词测试

在聊天框中输入以下任意消息：

```
发张图片
画个图看看
生成一张照片
来张好看的照片
给我看图
有图片吗
配个图
```

### 方法 2：查看控制台日志

1. 打开浏览器（推荐 Chrome）
2. 访问 http://localhost:5000
3. 按 F12 打开开发者工具
4. 切换到 "Console" 标签
5. 发送消息包含 "图片" 或 "图"

**正常日志输出：**
```javascript
[ChatScreen] 媒体类型检测: {
  输入: "发张图片",
  视频生成: false,
  图片生成: true,  ✅ 应该是 true
  语音生成: false
}
[图像生成] 开始生成，prompt: 温馨浪漫的场景，适合情侣，发张图片
[图像生成] API 响应: {success: true, imageUrl: "https://..."}
[图像生成] 成功添加图片消息
```

**如果图片生成是 false：**
- 检查输入是否包含关键词
- 查看完整日志

---

## 🐛 故障排查

### 问题 1：没有生成图片

**检查项：**
- [ ] 输入是否包含关键词？（尝试 "发张图片"）
- [ ] 浏览器控制台是否有错误？
- [ ] 网络连接是否正常？

**解决方法：**
```javascript
// 在控制台手动触发测试
fetch('/api/image', {
  method: 'POST',
  headers: {'Content-Type': 'application/json'},
  body: JSON.stringify({
    prompt: '一只可爱的小猫',
    personalityId: 'ceo'
  })
}).then(r => r.json()).then(console.log)
```

### 问题 2：显示 "图片生成失败"

**查看控制台错误信息：**
- `API 密钥无效` → 检查 `.env.local` 配置
- `请求过于频繁` → 等待几分钟后重试
- `服务暂时不可用` → 检查网络连接

### 问题 3：总是回退到 Coze

**检查日志：**
- 看到 `[VolcengineImageProvider]` = 使用火山引擎 ✅
- 看到 `[CozeImageProvider]` = 火山引擎失败，回退到 Coze ⚠️

**如果总是 Coze：**
- 可能 API Key 无效
- 检查 `.env.local` 中的配置

---

## 🎯 推荐测试消息

### 简单测试
```
发张图片
画个图
生成照片
```

### 场景测试
```
发张我们在一起的图片
画个浪漫的场景
生成一张好看的照片给我
```

### 调试测试
```
有图吗
图呢
给我看看图片
配个插图
```

---

## 📊 验证要点

### ✅ 成功标志
1. 控制台显示 "图片生成: true"
2. 看到 "正在生成图片..." 提示
3. 聊天界面显示图片
4. 日志显示成功添加图片消息

### ❌ 失败标志
1. 控制台显示 "图片生成: false" → 关键词未触发
2. 显示错误消息 → API 调用失败
3. 长时间无响应 → 网络问题

---

## 🔍 实时监控

### 查看服务器日志

```bash
# 在新的终端窗口运行
cd "d:\图片\虚拟男友\projects"
tail -f .next/server/app/api/image/route.ts
```

### 查看所有 API 调用

在浏览器控制台运行：
```javascript
// 监听所有 fetch 请求
const originalFetch = window.fetch;
window.fetch = function(...args) {
  console.log('[API 调用]', args[0]);
  return originalFetch.apply(this, args);
}
```

---

## 💡 提示

1. **关键词优先级**：视频 > 图片 > 语音
2. **视频关键词会覆盖图片**：如 "纪念日"、"生日" 等会触发视频而非图片
3. **延迟时间**：图片在文字回复 1.5 秒后生成
4. **Provider 切换**：火山引擎失败会自动尝试 Coze

---

**修改完成时间：** 2026-04-09
**状态：** ✅ 已优化，准备测试
