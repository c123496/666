# 🔍 调试图像生成问题

## 请提供以下信息

### 1. 你输入了什么内容？
请告诉我你发送的具体消息是什么

### 2. 浏览器控制台（F12）显示什么？
打开浏览器控制台（按 F12），查看是否有红色错误信息

### 3. 是否看到 "正在生成图片..." 提示？
聊天界面上是否显示了加载状态

---

## 🚨 临时解决方案

### 方法 1：直接测试图像 API

打开浏览器，在控制台（F12 → Console）粘贴以下代码：

```javascript
fetch('/api/image', {
  method: 'POST',
  headers: {'Content-Type': 'application/json'},
  body: JSON.stringify({
    prompt: '一只可爱的小猫',
    personalityId: 'ceo'
  })
}).then(r => r.json()).then(console.log).catch(console.error)
```

### 方法 2：使用简单关键词

尝试以下任一消息：
- `发张图片`
- `图片`
- `图`
- `生成图`
- `画个图`

**注意：** 不要输入 "纪念日"、"生日" 等词，这些会触发视频而不是图片

---

## 🐛 常见问题

### 问题：视频优先级覆盖了图片

如果你的消息包含这些词，会生成视频而不是图片：
- 纪念日、生日、在一起、爱你、想你
- 特别、重要、难忘、结婚、求婚
- 情人节、圣诞、跨年、新年、浪漫

**解决方法：** 避免使用这些词

---

## 📝 快速测试步骤

1. 刷新页面（Ctrl + R）
2. 输入：`发张图片`（不要包含其他词）
3. 查看浏览器控制台（F12）
4. 查找以下日志：

```javascript
[ChatScreen] 媒体类型检测
[图像生成] 开始生成
```

如果没有看到这些日志，说明关键词检测失败
