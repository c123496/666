# 🚨 GPT-Image API Key 问题排查

## 当前状态
- ✅ API 地址正确：`https://api.evolink.ai/v1`
- ❌ API Key 无效：`sk-your-api-key-here`（请在 .env.local 中配置真实 key）
- ❌ 所有认证格式都失败

## 🔧 立即解决方案

### 方案 1: 重新生成 API Key（推荐）

1. **登录控制台**
   - 访问：https://api.evolink.ai
   - 检查账户状态和余额

2. **重新生成 Key**
   - 找到 API Keys 管理
   - 删除旧的 Key
   - 创建新的 API Key
   - 复制完整的 Key（确保没有空格）

3. **更新配置**
   - 将新的 Key 更新到：
     - `.env.local` 文件
     - 或者直接提供给我，我来更新

### 方案 2: 使用临时替代方案

在获取有效 API Key 之前，可以使用免费的图片服务：

1. **Unsplash API** - 免费，高质量图片
2. **Picsum Photos** - 随机图片服务
3. **Lorem Flickr** - 基于关键词的图片

### 方案 3: 检查现有 Key

如果你想继续使用当前 Key，请检查：

- [ ] Key 是否完整复制（54字符）
- [ ] 是否有前后空格
- [ ] 账户是否有余额
- [ ] Key 是否在控制台中显示为"活跃"状态
- [ ] 是否需要等待生效时间

## 📝 如何提供新的 API Key

**方式 1: 直接告诉我**
```
"新的 API Key 是: sk-xxxxxxxxxxxxx"
```

**方式 2: 更新环境变量文件**
在 `.env.local` 文件中更新：
```env
GPT_IMAGE_API_KEY=你的新Key
```

**方式 3: 在控制台中截图**
我可以帮你从截图读取正确的信息

## ⚡ 临时解决方案

如果你需要立即测试项目，我可以：
1. 启用临时图片服务（使用 Unsplash 等）
2. 让项目继续运行，等新 Key 就绪后再切换

---

**当前项目状态：**
- 🟢 聊天功能：正常运行
- 🔴 图片生成：等待有效 API Key
- 🟡 其他功能：正常工作

请告诉我你希望采取哪种方案，我会立即实施！
